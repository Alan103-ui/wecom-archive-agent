# ---- Web 框架 ----
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# ---- 数据层（SQLite / PostgreSQL 双兼容）----
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
# 生产切 PostgreSQL 时启用（本地 SQLite 无需安装）
# psycopg[binary]>=3.1

# ---- 企业微信会话存档解密 ----
# encrypt_random_key 为 RSA-2048 / PKCS#1 v1.5 加密，需用企业私钥解密
pycryptodome>=3.20

# ---- OCR ----
# RapidOCR = PP-OCR 模型 + ONNXRuntime，纯 CPU 可跑，免 Paddle 依赖地狱
rapidocr-onnxruntime>=1.3
Pillow>=10.0
# PDF 转图片 / 提取文本层
pymupdf>=1.24

# ---- LLM 结构化抽取 ----
httpx>=0.27

# ---- 定时调度 ----
apscheduler>=3.10

# ---- 导出 ----
openpyxl>=3.1

# ---- 测试 ----
pytest>=8.0
pytest-asyncio>=0.23
